# Cargo's workspace target dir, plus the per-product target/book dirs the
# portfolio and assessments mdBooks build into.
target/

# Generated local state (demo corpus builds, tool caches) — never committed.
.como/

# The golden-export byte-pin's on-mismatch dump (review, promote to the
# golden, delete) — written next to the canonical fixture.
contract/fixtures/*.actual.yaml
